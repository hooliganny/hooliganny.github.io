---
interface Props {
  name: string;
}
const { name } = Astro.props;
---

{/* For some reason, this back button isn't persisting */}
<div id="back" class="cursor-pointer" transition:persist>
  {name}
</div>

<script>
  const button = document.querySelector("#back");
  console.log(button);
  button?.addEventListener("click", () => {
    history.back();
  });
</script>
